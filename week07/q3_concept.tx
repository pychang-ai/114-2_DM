姓名：顏于萱
學號：C113181107

Q1：決策樹為什麼容易過擬合？
    max_depth 如何幫助控制過擬合？
    除了 max_depth，還有什麼參數可以用來防止過擬合？
A1：
1. 容易過擬合的原因：決策樹在訓練時會非線性地不斷切割特徵空間。如果不加限制，它會強行分支直到每一個葉子節點都完全純淨（例如切到只剩 1 個樣本）。這會導致模型連訓練資料中的隨機雜訊、離群值（Outliers）與特定雜訊都死記下來，喪失了對未知新資料的泛化能力。
2. max_depth 的控制原理：max_depth 限制了樹的最大生長深度，屬於一種「預剪枝（Pre-pruning）」技術。它能強制模型在擷取到最主要的全局特徵後就停止分裂，防止樹走入過於瑣碎、只適用於少數樣本的極端分支，進而降低模型複雜度。
3. 其他防止過擬合的參數（Scikit-learn）：
   * min_samples_split：限制一個節點至少需要有多少樣本數才允許繼續分裂。
   * min_samples_leaf：限制一個葉子節點至少必須包含的最小樣本數，避免產生只為少數雜訊服務的特端葉子。
   * max_leaf_nodes：限制整棵樹最大容許的葉子節點總數。
   * ccp_alpha：用於代價複雜度剪枝（Cost-Complexity Pruning）的參數，透過增加懲罰項進行後剪枝。

Q2：決策樹的 feature_importances_ 代表什麼意義？
    如果某個特徵的重要性為 0，代表什麼？
    你覺得特徵重要性分析對實務應用有什麼幫助？
A2：
1. 代表意義：它代表各特徵在決策樹分裂過程中所做出的貢獻度。在 Scikit-learn 中，它是指該特徵在所有節點分裂時，所帶來的基尼不純度（Gini Impurity）或資訊熵（Entropy）的「總減少量（加權平均）」。數值越高，代表該特徵對分類的決定性越強。
2. 重要性為 0 的含意：代表在該棵樹的建立過程中，該特徵「從未被選用」來作為任何節點的分裂依據。這可能是因為它本身與目標標籤無關，或是它的資訊已被其他相關性更高的特徵完全取代。
3. 實務應用的幫助：
   * 特徵篩選與降維：在面對成百上千個欄位的大數據時，可直接剔除權重為 0 或極低的特徵，減少運算成本、加快預測速度。
   * 業務洞察與可解釋性（Explainable AI）：例如在第 2 題的船舶故障預測中，若分析出 `flavanoids` 或 `vibration_level`（振動強度）重要性極高，工程師就能以此作為排查故障、制定預防性保養計畫的科學依據。

Q3：這週你比較了多種分類模型。在實際應用中，選擇模型時
    除了準確率之外，還應該考慮哪些因素？請至少提出兩點。
A3：
在實務部署中，模型評估不能只看單一的 Accuracy（準確率），還必須綜合評估以下關鍵因素：
1. 模型的可解釋性（Interpretability）：
   在醫療診斷、金融貸款審核或工業安全領域，我們不僅需要知道預測結果，更需要知道「為什麼」。決策樹與邏輯斯迴歸（Logistic Regression）具有極高的透明度與可解釋性，容易向客戶或稽核單位說明決策邏輯；而 SVM 或深度學習等「黑盒子模型」則較難做到這點。
2. 預測延遲與計算資源（Inference Speed & Resource Constraints）：
   如果模型要部署在邊緣裝置（如船舶感測器、手機晶片、IoT 設備）或需要即時響應的系統（如自動駕駛、高頻交易），預測速度就至關重要。KNN 模型在預測時需要計算與所有訓練樣本的距離，記憶體消耗大且速度慢；相反地，決策樹一旦訓練完成，預測時只需做簡單的條件判斷，執行速度極快。
3. 資料不平衡的承受度與錯誤代價（Data Imbalance & Misclassification Cost）：
   以設備故障預測（第 2 題）為例，通常「未故障（0）」的資料遠多於「故障（1）」。如果模型把所有設備都猜未故障，雖然準確率很高，但漏報任何一個故障都會導致嚴重的經濟損失。此時我們應該改以 Recall（召回率）或 F1-score 作為評估依據，而非盲目追求 Accuracy。
