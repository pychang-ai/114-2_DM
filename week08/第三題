姓名：陳芊昀
學號：c113181143

Q1：為什麼交叉驗證比單次 train_test_split 更能反映模型的真實效能？
    如果只做一次 train_test_split，可能會遇到什麼問題？
A1：交叉驗證比單次切割更能反映模型真實效能的原因：

    1. 減少隨機性影響：單次切割的結果會受到資料分割方式的隨機性影響，
       可能因為運氣好或不好而得到偏頗的評估結果。
    
    2. 充分利用資料：交叉驗證讓每一筆資料都有機會成為訓練集和測試集，
       能更全面地評估模型在不同資料子集上的表現。
    
    3. 降低過擬合風險：多次測試能發現模型是否對特定資料分割過度擬合。
    
    如果只做一次 train_test_split 可能遇到的問題：
    - 評估結果不穩定：換一組 random_state 可能得到完全不同的分數
    - 浪費資料：若資料量少，切割後訓練集可能不足以代表母體
    - 無法發現模型的變異性：不知道模型對不同資料的敏感程度
    - 可能選到在特定分割表現好但泛化能力差的模型


Q2：GridSearchCV 中的 cv=5 代表什麼意思？
    如果參數搜尋空間有 4 個 C 值 × 4 個 gamma 值 × 2 個 kernel，
    加上 cv=5，總共需要訓練幾次模型？請列出計算過程。
A2：GridSearchCV 中的 cv=5 代表使用 5 折交叉驗證（5-Fold Cross-Validation）。
    
    計算過程：
    - 參數組合數 = 4 (C) × 4 (gamma) × 2 (kernel) = 32 種組合
    - 每種參數組合需要進行 5 折交叉驗證 = 訓練 5 次模型
    - 總訓練次數 = 32 種組合 × 5 折 = 160 次
    
    因此總共需要訓練 160 次模型。

Q3：SVM 的參數 C 控制什麼？C 值很大和很小分別會造成什麼效果？
    這跟過擬合/欠擬合有什麼關係？
A3：SVM 的參數 C（正規化參數）控制「錯誤分類的懲罰程度」：
    
    C 值很大（高懲罰）：
    - 模型會盡量正確分類每一個訓練樣本
    - 決策邊界會變得複雜，試圖完美區分所有資料點
    - 容易導致「過擬合」（overfitting）：對訓練資料表現很好，
      但對未見過的測試資料泛化能力差
    - 對雜訊和離群值非常敏感
    
    C 值很小（低懲罰）：
    - 允許較多的分類錯誤，決策邊界較平滑、簡單
    - 容易導致「欠擬合」（underfitting）：模型過於簡單，
      無法捕捉資料的真實模式
    - 對離群值較不敏感，有較好的泛化能力
    - 但若太小可能連基本的區分都做不好
    
    與過擬合/欠擬合的關係：
    - 過擬合 ←→ 高 C 值（複雜邊界）
    - 欠擬合 ←→ 低 C 值（簡單邊界）
    - 最佳 C 值需要在兩者之間取得平衡，通常透過交叉驗證來尋找
