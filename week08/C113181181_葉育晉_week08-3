姓名：葉育晉
學號：C113181181

Q1：為什麼交叉驗證比單次 train_test_split 更能反映模型的真實效能？
    如果只做一次 train_test_split，可能會遇到什麼問題？
A1：因為使用單次 train_test_split 時，模型的效果會高度依賴於數據分割的隨機性（也就是所謂的「抽樣偏差」）。
    如果只做一次，這代表數據集中可能有某些重要的模式或邊界案例沒有被納入測試集，導致評估不全面。

Q2：GridSearchCV 中的 cv=5 代表什麼意思？
    如果參數搜尋空間有 4 個 C 值 × 4 個 gamma 值 × 2 個 kernel，
    加上 cv=5，總共需要訓練幾次模型？請列出計算過程。
A2：這代表使用 5-fold Cross-Validation（5 折交叉驗證)，系統會將訓練數據集平均分成 5 等份。  
    計算參數組合數：4(C 值) * 4(gamma值) * 2 (kernel)= 32種參數組合。
    計算總訓練次數：對於每一種參數組合，都需要執行 5 折交叉驗證。 32(組合) * 5(折數) = 160次

Q3：SVM 的參數 C 控制什麼？C 值很大和很小分別會造成什麼效果？
    這跟過擬合/欠擬合有什麼關係？
A3：參數 C 是 SVM 的懲罰係數（Regularization parameter），用來控制模型對於「錯誤分類」的容忍度。
C 值很大 (Hard Margin)：
效果： 模型會傾向於盡可能將所有訓練資料正確分類，甚至不惜建立非常複雜、彎曲的決策邊界。
風險： 因為模型對誤差的容忍度極低，容易把數據中的雜訊也學習進去，導致過擬合 (Overfitting)，在訓練集表現完美，但在測試集表現較差。
C 值很小 (Soft Margin)：
效果： 模型容許較多的錯誤分類，傾向於建立一個更平滑、更寬的決策邊界，忽略部分離群值（Outliers）。
風險： 如果 C 設定過小，模型會變得太「大而化之」，導致無法捕捉數據的特徵，引發欠擬合 (Underfitting)。
總結：
大 C = 高複雜度 = 容易過擬合
小 C = 低複雜度 = 容易欠擬合
