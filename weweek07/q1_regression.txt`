姓名：謝佳芯
學號：c11381127

=== 完整程式碼 ===
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error, r2_score

df = pd.read_csv('data/Ship_Performance_Dataset.csv')

=== 簡單迴歸結果 ===
使用特徵：Engine_Power_kW
斜率：???   截距：???
迴歸公式：Cost = ??? × Engine_Power_kW + ???
訓練 R²：???   測試 R²：???
訓練 RMSE：??? 測試 RMSE：???

=== 多元迴歸結果 ===
各特徵迴歸係數：包含引擎功率、載貨量、航行距離、航速與吃水深度五項係數。
訓練/測試 R² 與 RMSE：（請執行程式後填入運算結果）

=== 簡單 vs 多元比較 ===
R² 與 RMSE 的差異：
通常多元迴歸的 R² 會較高，RMSE 會較低。
原因：簡單迴歸僅考慮「引擎功率」，而營運成本通常受到多個因素（如航行距離、載貨量等）的共同影響。多元迴歸能捕捉更多影響成本的變因，減少未被解釋的殘差，從而提升模型的預測績效。

影響最大的特徵：
若資料已標準化，可直接比較係數絕對值大小。
若未標準化，則須觀察各特徵係數對目標值的貢獻程度。在船舶營運中，通常 Engine_Power_kW（引擎功率） 或 Distance_Traveled_nm（航行距離） 會是關鍵影響因子。
