# W14-Exercise_C113187221

## Error01
ValueError: could not convert string to float: 'sales' 表示 df.corr() 方法嘗試對非數值型資料（特別是 'sales' 欄位，它包含字串值）計算相關性。corr() 方法只能處理數值型資料。
為了解決這個問題，我修改了程式碼，在 df.corr() 方法中添加了 numeric_only=True。這樣做會指示 corr() 方法只考慮資料框中的數值型欄位來計算相關性，從而避免了嘗試將字串轉換為浮點數的錯誤。
```
#sns.heatmap(df.corr().round(2), annot=True, cmap='coolwarm');
sns.heatmap(df.corr(numeric_only=True).round(2), annot=True, cmap='coolwarm');
```
這行程式碼的目的是繪製一個相關性熱力圖（heatmap），用來視覺化資料框 df 中各數值型欄位之間的相關性。具體來說：
- df.corr(numeric_only=True): 計算資料框 df 中所有數值型欄位之間的皮爾遜相關係數。numeric_only=True 確保只對數值型欄位進行計算，避免非數值型欄位導致的錯誤。
- .round(2): 將相關係數的結果四捨五入到小數點後兩位，使圖表更易讀
- sns.heatmap(...): 使用 Seaborn 函式庫中的 heatmap 函式來繪製熱力圖。
- annot=True: 在熱力圖的每個單元格中顯示相關係數的數值。
- cmap='coolwarm': 設定熱力圖的顏色映射。'coolwarm' 是一種常見的色譜，通常用於表示正相關（暖色）和負相關（冷色），並且中間色表示接近零的相關性。

## Error02
ValueError: could not convert string to float: 'sales' 表示 corrwith() 函數無法處理非數值資料。在計算相關性之前，你需要先選擇數值型的欄位。
```
# df.drop('left', axis=1).corrwith(df['left']).round(2)
df.drop('left', axis=1).select_dtypes(include=np.number).corrwith(df['left']).round(2)
```
這些結果顯示了各個數值型特徵與員工是否離職（left 欄位）的相關係數：
- satisfaction_level (滿意度) 與離職呈現負相關 (-0.39)，表示滿意度越低，離職傾向越高。
- last_evaluation (上次考核)、number_project (專案數量) 和 average_montly_hours (平均月工時) 與離職呈現微弱的正相關，但相關性很低。
- time_spend_company (在公司年資) 與離職呈現正相關 (0.14)，表示在公司待得越久，離職的可能性越高，這可能與達到一定年資後尋求新機會有關。
- Work_accident (工作事故) 和 promotion_last_5years (五年內是否晉升) 與離職呈現負相關，表示有工作事故或晉升過的員工離職傾向較低。

## Error03
AttributeError: 'float' object has no attribute 'round' 的意思是，你嘗試在一個浮點數上呼叫 round() 方法，但浮點數本身沒有這個方法。accuracy_score() 函數返回的是一個浮點數。在 Python 中，如果你想對浮點數進行四捨五入，應該使用內建的 round() 函數。
```
# print('正確率：', accuracy_score(y_test, y_pred).round(2))
print('正確率：', round(accuracy_score(y_test, y_pred), 2))
```
- 正確率 (Accuracy)： 0.76。這表示模型有 76% 的時間做出了正確的預測。由於這個 DummyClassifier 策略是「預測出現次數最多的類別」（在您的資料中，這是「員工未離職」），所以它會將所有員工都預測為「未離職」。這個 0.76 的正確率，正是數據集中未離職員工的比例。
- 混亂矩陣 (Confusion Matrix)：
[[3428    0]
 [1072    0]]
第一行 [3428 0] 表示有 3428 名實際未離職的員工被正確預測為未離職 (True Negatives)，0 名實際未離職的員工被錯誤預測為離職 (False Positives)。
第二行 [1072 0] 表示有 1072 名實際離職的員工被錯誤預測為未離職 (False Negatives)，0 名實際離職的員工被正確預測為離職 (True Positives)。
這再次證明了模型只預測了「未離職」這個類別。
- 綜合報告 (Classification Report)：
類別 0 (未離職)： 精準度 (precision) 0.76，召回率 (recall) 1.00，F1-score 0.86。這表示模型在預測未離職員工方面表現良好，因為它總是預測這個類別，並且這個類別確實佔了大多數。
類別 1 (離職)： 精準度 (precision) 0.00，召回率 (recall) 0.00，F1-score 0.00。這是一個非常重要的結果，表示這個基礎模型完全無法識別出任何會離職的員工。因為模型從未預測任何員工會離職，所以對於實際離職員工的精準度和召回率都是零。

## Error04
AttributeError: 'float' object has no attribute 'round' 的意思是，你嘗試在一個浮點數上呼叫 round() 方法，但浮點數本身沒有這個方法。score 變數是一個浮點數，在 Python 中，如果你想對浮點數進行四捨五入，應該使用內建的 round() 函數。
```
# model_pl = Pipeline([
    ('preprocess', data_pl),
    ('model', LogisticRegression())
])
param_grid = {'model':[LogisticRegression(), SVC(), 
model_pl = Pipeline(
    [('preprocess', data_pl),
     ('model', LogisticRegression())
    ])
param_grid = {'model':[LogisticRegression(), SVC(),

# print('訓練集交叉驗證的最佳結果', gs.best_score_.round(3))
print('測試集的結果', round(score, 3))
print('訓練集交叉驗證的最佳結果', gs.best_score_.round(3))
print('測試集的結果', round(score, 3))
```
- 初始模型（邏輯迴歸、SVC、KNN、決策樹）的 GridSearchCV 處理已成功完成。以下是相關結果：
- 最佳參數： GridSearchCV 所找到的最佳模型是一個深度為 10 的 DecisionTreeClassifier 模型。
- 最佳交叉驗證訓練分數：透過交叉驗證所獲得的最佳訓練分數約為 0.978。
- 測試集分數：在測試集上的分數約為 0.977。
- Confusion Matrix:  混淆矩陣：
[[3394   34]
 [  68 1004]]
 此混淆矩陣顯示：
 3394 個真正的負面預測結果（即：系統正確預測了這些員工不會離職）。
 34 例誤判情況（即員工被錯誤地預測為會離職）。
 68 例錯誤的負面預測結果（即員工被錯誤地判斷為不會離職）。
 1004 個真正的正面預測結果（即，正確預測了那些會離職的員工）。
